class Solution {
    public String multiply(String num1, String num2) {
        int tmp1 = Integer.parseInt(num1);
        int tmp2 = Integer.parseInt(num2);

        return String.valueOf(tmp1 * tmp2);
    }
}